---
+doctype: dist
name: Badge-Depot-Plugin-Travis

+prereqs:
  runtime:
    requires:
     - perl: 5.014000
     - Moose: 2.0000
     - Badge::Depot
     - namespace::autoclean
     - Types::Standard

+plugins:
 - +config: Author::CSSON

---
+doctype: weaver

+plugins:
 - +config: Author::CSSON

 - +remove_plugin: Splint
